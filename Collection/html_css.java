Basic Structure

<!DOCTYPE html> → Defines the document type (HTML5).

<html> → Root element of the HTML document.

<head> → Contains metadata (title, styles, scripts, SEO info).

<title> → Sets the page title (shown in the browser tab).

<body> → Contains the content that appears on the webpage.

🔹 Text & Headings

<h1> to <h6> → Headings (h1 is the largest, h6 the smallest).

<p> → Paragraph of text.

<br> → Line break.

<hr> → Horizontal line (thematic break).

🔹 Formatting Text

<b> or <strong> → Bold text (strong has semantic meaning = important).

<i> or <em> → Italic text (em = emphasized).

<u> → Underlined text.

<a> → Hyperlink (example: <a href="url">Click me</a>).

<img> → Displays an image (src for URL, alt for description).

<ul> → Unordered list (bullet points).

<ol> → Ordered list (numbered).

<li> → List item.

<dl> → Description list.

<dt> → Term (name).

<dd> → Description (details).

🔹 Tables

<table> → Creates a table.

<tr> → Table row.

<td> → Table data (cell).

<th> → Table header (bold, centered by default).


🔹 Forms & Inputs

<form> → Creates a form.

<input> → Input field (text, number, checkbox, etc.).

<button> → Clickable button.

<select> → Dropdown menu.

<option> → Option inside dropdown.

<label> → Label for a form element.

🔹 Other Useful Tags

<div> → Generic container (block-level).

<style> → Internal CSS.

<script> → JavaScript code.